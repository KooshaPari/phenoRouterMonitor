# phenosdk-domain — Domain model for PhenoSDK

[package]
name = "agileplus-domain"
version = "0.2.0"
edition = "2021"
authors = ["Phenotype Team"]
description = "Domain model for PhenoSDK"
license = "MIT"

[lib]
path = "src/lib.rs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json.workspace = true
thiserror.workspace = true
uuid.workspace = true
chrono.workspace = true
