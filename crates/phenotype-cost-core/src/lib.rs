//! Cost calculation, pricing models, and budget enforcement for LLM operations.
//!
//! This crate is a workspace placeholder; expand with concrete cost types and policies.

#![forbid(unsafe_code)]

/// Placeholder until cost models are implemented.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub struct CostPlaceholder;
