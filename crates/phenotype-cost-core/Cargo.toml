[package]
name = "phenotype-cost-core"
version = "0.2.0"
edition = "2021"
license = "MIT"
description = "Cost modeling, analysis, and budgeting for computational operations."

[lib]
path = "src/lib.rs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
thiserror = "2.0"
