[package]
name = "phenotype-contracts"
version = "0.2.0"
edition = "2021"
license = "MIT"
description = "Shared contracts and trait definitions for Phenotype crates"

[lib]
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
