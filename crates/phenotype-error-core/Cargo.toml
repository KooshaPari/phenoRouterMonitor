[package]
name = "phenotype-error-core"
version = "0.2.0"
edition = "2021"
license = "MIT"
description = "Error types for Phenotype"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
thiserror = "2.0"
