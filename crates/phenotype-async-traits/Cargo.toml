[package]
name = "phenotype-async-traits"
version = "0.2.0"
edition = "2021"
license = "MIT"
description = "Reusable async trait helper utilities for Phenotype crates."

[lib]
path = "src/lib.rs"

[dependencies]
async-trait = "0.1"
