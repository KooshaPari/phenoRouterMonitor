[package]
name = "phenotype-git-core"
version = "0.2.0"
edition = "2021"
license = "MIT"

[lib]
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "2"
gix = "0.71"

[dev-dependencies]
tokio = { version = "1", features = ["full"] }
