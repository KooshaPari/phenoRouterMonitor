[package]
name = "phenotype-telemetry"
version = "0.1.0"
edition = "2021"

[dependencies]
phenotype-error-core = { path = "../phenotype-error-core" }
tracing = "0.1"
tracing-subscriber = "0.3"

[lib]
path = "src/lib.rs"
