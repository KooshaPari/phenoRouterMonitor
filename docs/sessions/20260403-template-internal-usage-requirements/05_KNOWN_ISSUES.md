# Known Issues

- `docs/worklogs/README.md` contains unresolved merge markers and should not be treated as canonical until repaired.
- Some template session docs still describe root `template-lang-*` placeholders that no longer match the active filesystem layout.
- Contract coverage remains incomplete for some active language layers (`phenoTemplates/templates/python`, `phenoTemplates/templates/rust`).
