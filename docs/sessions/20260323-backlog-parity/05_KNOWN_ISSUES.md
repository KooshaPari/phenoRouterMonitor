# Known Issues

- No test or build validation has been run yet.
- MCP backlog batch import/pop/get/status use the integrations service; runtime validation is still
  pending.
- External GitHub branch protection / ruleset configuration is still outside the repository.
- The session docs now reflect the current backlog import and release hygiene state, so the main
  remaining risk is code-level validation rather than missing workflow semantics.
