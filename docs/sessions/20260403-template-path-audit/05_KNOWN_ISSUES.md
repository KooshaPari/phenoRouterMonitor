# Known Issues

- Root contains seven empty `template-lang-*` directories that appear to be placeholders rather than populated template repos.
- `template-program-ops/scripts/validate-foundation.sh` still references two nonexistent root directories: `template-lang-mojo/` and `template-lang-swift/`.
- Session docs under `docs/sessions/20260403-template-registry-consolidation-research/` and `template-program-ops/docs/sessions/2026-03-01-template-workflow-hardening/` preserve an older inventory and are now stale relative to the current filesystem.
