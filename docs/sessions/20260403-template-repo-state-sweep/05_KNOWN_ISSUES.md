# Known issues

- The shelf still has seven empty root placeholder directories: `template-lang-elixir-hex`, `template-lang-go`, `template-lang-kotlin`, `template-lang-python`, `template-lang-rust`, `template-lang-typescript`, and `template-lang-zig`.
- `phenoInfrakit/` also carries empty placeholder directories for `template-lang-swift`, `template-lang-mojo`, and `template-lang-rust`.
- Stale docs under older sessions still refer to root `template-lang-swift` and `template-lang-mojo` paths.
