# Research

- Root-level `template-*` directories currently include four real git repos (`template-commons`, `template-domain-service-api`, `template-domain-webapp`, `template-program-ops`) and seven empty placeholder `template-lang-*` directories.
- `template-lang-swift` and `template-lang-mojo` are absent from the shelf root.
- Canonical starter template content for Swift and Mojo exists under `phenoTemplates/templates/{swift,mojo}` with manifest, reconcile rules, Taskfile, and smoke script present.
- `phenoTemplates/templates/rust` exists only as a partial stub and lacks contract artifacts.
- Empty placeholder directories for `template-lang-swift`, `template-lang-mojo`, and `template-lang-rust` also exist under `phenoInfrakit/`.
